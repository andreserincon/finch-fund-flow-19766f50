# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables. The .env.*.local variants are already covered by
# *.local above. The committed .env holds only publishable client (VITE_) values;
# never put a server-side secret in it.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local tooling (worktrees, launch config)
.claude/
